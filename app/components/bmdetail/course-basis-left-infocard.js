import Component from '@ember/component';

export default Component.extend({
    positionalParams: ['infoCardData'],
    tagName: '',
    // haveAge: true,
    // haveLength: true,
    // init() {
    //     this._super(...arguments);
    //     if(this.infoCardData.alb == -1 && this.infoCardData.aub == -1) {
    //         this.set('haveAge', false);
    //     }
    //     if (this.infoCardData.length == -1) {
    //         this.set('haveLength', false);
    //     }
    // },
});
