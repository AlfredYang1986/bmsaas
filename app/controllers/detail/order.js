import Controller from '@ember/controller';
// import { computed } from '@ember/object';

export default Controller.extend({
    // total_discount: computed(function(){
    //     debugger
    //     let result = 0;
    //     this.model.trans.attachables.forEach(elem => {
    //         result += elem.preferentialPrice;
    //     });
    //     return result;
    // }),
});
