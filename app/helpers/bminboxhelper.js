import { helper } from '@ember/component/helper';

export function bminboxhelper(params/*, hash*/) {
	console.log('helper inbox count');
	// debugger
	// let s = params[0];
	return 0; //s.queryUnhandled();
}

export default helper(bminboxhelper);
