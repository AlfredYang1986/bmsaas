<ul class="left_bar">
    {{#each menuList as |item|}}
        {{left-menu-item icon=item.icon title=item.title expendable=item.expond pageUrl=item.pageUrl isInbox=item.isInbox isSpace=item.isSpace}}
    {{/each}}
</ul>

{{yield}}
