<div >
    {{nav-top}}
    {{left-menu}}
</div>

<div class="course_content_top" >
    {{#layout-push direction="col" classNames="course_content"}}
        {{#layout-push direction="row"}}
            {{#link-to 'edit.course' 'course/push' classNames='course_add_btn'}}
                + 添加课程
            {{/link-to}}
        {{/layout-push}}
        {{#layout-push direction="row" wrap=true classNames="card-wrap"}}
            {{#each model.course as |item|}}
                <div class="line_container_warp course_card_container" >
                    {{course-card onCourseCardClicked=(action 'cardClicked') course=item}}
                </div>
            {{/each}}
        {{/layout-push}}

    {{/layout-push}}
</div>

{{outlet}}
